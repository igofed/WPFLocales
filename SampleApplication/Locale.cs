namespace SampleApplication
{
    static class Locale
    {
        public enum SampleApplication   
        {
            StateLabelContent,
            StateOkText,
            StateErrorText,
            ChangeStateButtonContent,
            LocaleLabelContent
        }
    }
}
